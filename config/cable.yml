development:
  adapter: redis
  url: redis://localhost:6379/1

test:
  adapter: test

production:
  adapter: redis
  url: <%= Rails.application.credentials.redis %>
  channel_prefix: simplero_social_production
